# Travello AI Backend — Python 3.11+
# Install: pip install -r requirements.txt

# ─ Web framework 
fastapi==0.115.6
uvicorn[standard]==0.32.1          # includes websockets + uvloop for speed

# ─ Supabase
supabase==2.15.3                   # supabase-py client (anon + service role); 2.15+ allows httpx>=0.28 (needed by google-genai)

# ─ Direct DB (asyncpg) 
asyncpg==0.30.0                    # async PostgreSQL driver for raw queries

# ─ Auth / JWT 
python-jose[cryptography]==3.3.0   # JWT decode (verifies Supabase tokens)
passlib[bcrypt]==1.7.4             # bcrypt for OTP hashing
bcrypt==4.2.1                      # explicit pin — passlib uses this

# ─ HTTP client 

httpx==0.28.1                      # async HTTP; pinned — satisfies both supabase 2.15 and google-genai

# ─ Environment / config 
python-dotenv==1.0.1               # load .env in development
pydantic-settings==2.7.0           # typed settings from env vars

# ─ Serialisation 
orjson==3.10.12                    # fast JSON - FastAPI picks it up automatically

# Google Gemini (new SDK — replaces deprecated google-generativeai)
google-genai==2.14.0               # pinned — unpinned floated to a build that broke httpx resolution

# Groq (free LLM API — Llama 3.3 70B, no billing required)
groq>=0.13.0
